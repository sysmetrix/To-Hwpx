# To HWPX 디자인 지침

이 문서는 To HWPX의 화면을 고칠 때 유지할 공통 기준이다. 목표는 화려한 랜딩페이지가 아니라, 사용자가 변환 전 상태를 빠르게 이해하고 믿고 실행할 수 있는 문서 작업 도구의 품질이다.

## 디자인 원칙

- 첫 화면의 주 행동은 파일 선택이다. 보조 안내, 설치, 개인정보, 업데이트 버튼은 파일 선택보다 먼저 시선을 빼앗지 않는다.
- 변환 화면은 `입력 → 변환 문서 기본 설정 → 문서 세부 설정 → 실행 → 결과` 순서로 읽혀야 한다.
- 자주 바꾸는 값은 숨기지 않는다. 글꼴, 글꼴 크기, 용지 크기, 용지 방향은 변환 문서 기본 설정 영역에 항상 노출한다.
- 드문 값은 접는다. 줄 간격, 구분선, 상단 제목 블록, 페이지 여백, 자동 다운로드, 개발자용 IR 미리보기는 문서 세부 설정 안에 둔다.
- 설명은 짧게 쓰고, 사용자가 지금 어떤 값으로 변환하는지 보여준다.

## 시각 톤

- 공공 문서, 보고서, 한글 문서 편집 맥락에 맞는 조용한 고급감을 우선한다.
- 큰 장식, 과한 그라데이션, 마케팅식 히어로, 중첩 카드, 장식용 배경 오브젝트는 쓰지 않는다.
- 패널 안에서는 두꺼운 카드보다 얇은 구분선, 간결한 헤더, 안정적인 입력 밀도로 위계를 만든다.
- 색상은 상태와 행동을 구분하는 데 쓴다. 한 화면을 한 가지 색 계열로만 밀지 않는다.
- 버튼 문구는 결과와 연결한다. 예: `이 설정으로 HWPX 만들기`.

## 변환기 패널 구조

권장 구조:

```text
파일 선택

문서 제목
포맷 보존/확인 안내

변환 문서 기본 설정
[한글 폰트] [글꼴 크기] [용지 크기] [용지 방향]

문서 세부 설정
줄 간격 / 구분선 / 상단 제목 블록 / 페이지 여백 / 자동 다운로드

[이 설정으로 HWPX 만들기]
```

금지 구조:

```text
파일 선택
문서 제목
접힌 고급 설정 안에 주요 옵션 전체
변환 시작
```

## 브랜드/디자인 토큰

- 브랜드 컨셉은 A안 "조용한 공공 신뢰"로 확정됨(2026-07). 로고는 `icons/app-icon.svg`(변환 화살표 문서 마크), 탐색 과정은 `icons/brand-concept/`에 보관.
- 제목·강조 텍스트에 그라데이션을 쓰지 않는다. 단일 `--c-primary`로 통일한다(과거 `.hero-title-accent` 그라데이션은 이 원칙과 충돌해 제거함, v4.10.14).
- 글자 크기는 새 컴포넌트부터 `--fs-2xs`~`--fs-3xl` 토큰을 쓴다(`style.css` `:root` 참조). 기존 선택자의 하드코딩된 `font-size`(0.78/0.8/0.82rem처럼 구분 안 되는 값들)는 일괄 치환하지 않고, 그 컴포넌트를 어차피 수정할 때 기회적으로 옮긴다.
- pill 형태(`border-radius: 50px`)는 화면당 가장 중요한 단일 행동(예: 변환하기 버튼)에만 쓴다. 여러 버튼에 동시에 적용하지 않는다 — 위계 신호가 사라진다.

## 컴포넌트 기준

- 드롭존은 가장 강한 입력 affordance를 가진다. 선택 후에는 파일 상태와 형식이 바로 보여야 한다.
- 포맷 힌트는 보존/확인 정보를 알려주되, 설정 영역 안내와 경쟁하지 않는다.
- 변환 문서 기본 설정 영역은 접지 않는다. 현재값 요약과 실제 컨트롤을 함께 보여준다.
- 문서 세부 설정 summary는 보조 기능임을 드러낸다. `더 보기` 수준의 낮은 위계가 적절하다.
- 가로 구분선은 실무 문서에서 쓰지 않는 경우가 많으므로 기본값은 `숨김`으로 둔다.
- 페이지 여백은 숫자 입력만 나열하지 않는다. 종이 미니맵, 본문 영역, 머리말/꼬리말 영역을 함께 보여주어 방향을 직관적으로 이해하게 한다.
- 모바일에서는 4열 입력을 1열 또는 2열로 줄이고, 텍스트가 버튼/입력 안에서 넘치지 않게 한다.

## 문구 기준

- `고급 설정`이라는 표현은 초심자에게 숨은 기능처럼 보이므로 피한다.
- 기본 표현은 `변환 문서 기본 설정`, 보조 접힘 영역은 `문서 세부 설정`을 쓴다.
- 기능 설명보다 현재 행동을 말한다.
  - 좋음: `이 값으로 HWPX가 만들어집니다.`
  - 피함: `글꼴, 글꼴 크기, 용지, 여백, 자동 다운로드를 설정합니다.`
- HWPX는 출력 형식으로 설명한다. 입력 가능 포맷처럼 쓰지 않는다.

## 롤백 가능한 작업 방식

- 옵션의 `id`, `name`, `data-*`, localStorage 키, 변환 state 이름은 바꾸지 않는다.
- UI 재배치와 변환 로직 변경은 같은 커밋에 섞지 않는다.
- 주요 설정 노출은 DOM 이동과 CSS 변경 중심으로 처리한다.
- 새 디자인이 마음에 들지 않으면 해당 UI 커밋만 `git revert`할 수 있어야 한다.
- 버전 범프와 changelog는 최종 확인 후 마지막 커밋에 반영한다.

## QA 체크리스트

- 첫 사용자가 5초 안에 글꼴/용지/방향 설정 위치를 찾을 수 있는가.
- 파일 선택 후 현재 변환값이 화면에 보이는가.
- 모바일 390px 폭에서 설정 컨트롤과 변환 버튼 텍스트가 넘치지 않는가.
- 다크 테마에서 구분선, 배지, 버튼 대비가 충분한가.
- `node qa/gate.js qa/fixtures/md_hwpx_test.md`와 `npm.cmd run test:golden`이 통과하는가.

## DOCX 파일별 품질 진단

DOCX는 문서마다 구조 복잡도와 입력 결함이 다르므로 포맷 전체를 하나의 고정 성공률로 표현하지 않는다. 품질 영역에는 `고정 점수 대신 파일별 실측`을 표시하고, 실제 변환 결과 카드에서 다음 근거를 보여준다.

- 입력 감사 상태: 정상, 자동 복구됨, 손실 가능, 변환 중단을 색상과 텍스트로 함께 구분한다.
- 입력 결함과 자동 복구: 결함 종류·건수와 수행한 복구를 숨기지 않는다.
- 구조 보존: 문단, 표, 행, 셀, 수동 줄바꿈을 `입력 → 출력` 형식으로 표시한다.
- 기대치: 페이지 설정·기본 타이포그래피·표 셀 블록 등 보존 항목과 섹션 전환·복잡 개체·Word/한컴 페이지 나눔 차이 등 제외 가능 항목을 함께 표시한다.

감사 패널은 성공 메시지보다 아래, 다운로드 행동보다 위에서 결과를 해석하는 근거로 동작한다. 숫자는 크기를 과장하지 않는 작은 계수 카드로 표시하고, 모바일에서는 2열로 줄여 가로 스크롤을 만들지 않는다. `recoverable-with-loss`처럼 상태 코드가 길어도 색상만으로 의미를 전달하지 않으며 한국어 상태 문구를 반드시 함께 둔다.

## 보조 산출물(HWP) 표시 기준

주 산출물은 항상 HWPX다. HWP 내보내기는 구버전 한/글 수신자를 위한 **보조 행동**이며, 화면에서도 그 위계를 지킨다.

- 주 다운로드(HWPX)는 채워진 버튼, HWP 내보내기는 점선 테두리의 보조 버튼으로 둔다. 두 버튼을 같은 무게로 그리지 않는다.
- HWP 버튼은 변환 성공 뒤에만 노출한다. 실패한 변환에는 보조 경로를 제안하지 않는다.
- 엔진(약 8MB WASM)은 버튼을 누른 뒤에만 내려받는다. 첫 화면 로딩에 얹지 않는다.
- 결과 문구는 **엔진이 실제로 보고한 사실만** 쓴다.
  - 손실 0건이면 `변환 엔진이 보고한 손실 항목은 없습니다.`
  - 보고서를 받지 못했으면 없다고 쓰지 않고 `확인할 수 없습니다`로 남긴다.
  - 쪽수가 달라졌으면 `쪽수 N → M`으로 변동을 그대로 적는다.
- HWP를 공공 제출 형식으로 권하지 않는다. 안내 문구에 `공공 문서 제출에는 HWPX를 쓰세요`를 유지한다. 2026년 10월부터 hwp 첨부가 제한되는 방향이므로, 이 보조 기능이 정책과 반대 신호를 주면 안 된다.
- 역방향 실패는 HWPX 다운로드에 영향을 주지 않는다. 오류는 보조 영역 안에서만 표시하고 결과 카드 전체를 실패로 바꾸지 않는다.

## 첫 화면 입력/출력 표기

- 드롭존 부제는 `입력`과 `출력`을 라벨로 분리해 두 줄로 적는다. 한 줄에 섞으면 HWPX가 입력 포맷처럼 읽힌다.
- 출력 줄은 `HWPX`를 먼저 쓰고 보조 산출물을 괄호로 덧붙인다: `출력 HWPX (HWP · PDF로도 저장 가능)`.
- 드롭존 바로 아래에 `파일은 브라우저 안에서만 변환됩니다. 서버에 업로드되지 않습니다.`를 둔다. 공문서·인사·계약 문서를 다루는 사용자에게 가장 강한 판매 논거이므로 모달 안에만 두지 않는다.
- 보조 문구를 `opacity`로 흐리지 않는다. axe `color-contrast`에 걸린다(실제로 `.drop-io-aux`에서 발생). 무게는 색 토큰과 굵기로만 조절한다.
- `.drop-trust`처럼 flex 컨테이너 안에 문장을 넣을 때는 문장 전체를 한 요소로 감싼다. 텍스트 노드와 `<b>`를 직접 flex 아이템으로 두면 390px 폭에서 어절마다 열로 쪼개진다.

## 디버그 훅 — `window.__tohwpxDebug`

`convertOneFile()`은 이번 렌더에 실제로 쓰인 IR과 렌더 옵션을 `window.__tohwpxDebug.lastRender`에 남긴다.

- **용도는 하나다.** `qa/core-parity-gate.js`가 이 값을 그대로 Node 코어에 넣어, 웹앱과 CLI·MCP가 같은 HWPX를 만드는지 검사한다.
- **옵션을 게이트에서 손으로 다시 적지 않는다.** 그러면 "같은 입력"이라는 전제가 깨져 비교가 무의미해진다. 반드시 앱이 렌더에 넘긴 값을 그대로 꺼내 간다.
- 이 훅이 비면 `test:core`가 조용히 무력화된다. 렌더 호출부를 옮기거나 감쌀 때 훅이 함께 따라가는지 확인한다.
- 문서 내용이 여기 담기지만 **브라우저 메모리 안에서만 존재**하며 전송되지 않는다. 분석 이벤트에 실리지 않도록 `ANALYTICS_SCHEMA` allowlist 밖에 둔다.

## 추론한 결과는 추론이라고 말한다 (PDF)

PDF는 글자 좌표만 담는 형식이라 문단·제목·표가 **추론 결과**다. 결과 카드는 그 사실과 근거를 함께 보여준다.

- PDF는 실제 입력 파서와 충실도 게이트가 있는 **베타 입력 포맷**이다. 입력 포맷 카드와 지원 현황에 표시하고 예정 포맷에 중복해 두지 않는다.

- 성공 배지 아래, 다운로드 행동 위에 `result-inference-note`를 둔다. 결과를 해석하는 근거로 동작해야 하므로 성공 메시지보다 아래다.
- 경고색(`--c-warning`)을 쓰되 실패로 읽히지 않게 한다. 변환은 성공했고, 다만 **원본 복제가 아니라는 것**을 알리는 자리다.
- 추론에 쓴 값을 그대로 보여준다: 본문 글자 크기, 제목으로 본 크기, 쪽수, 추론 결과 계수.
- **고정 백분율로 품질을 주장하지 않는다.** 문서마다 다르게 맞는다.
- 마지막 줄에 무엇을 못 가져왔는지(중첩 표·글꼴 이름·페이지 배치·표 밖 벡터 장식)와 대안(원본 문서 파일이 있으면 그쪽이 더 정확)을 적는다. 본문 그림·표 병합·굵게·기울임·글자색은 현재 보존되므로 제외 항목으로 쓰지 않는다.
- 스캔 PDF는 빈 문서를 만들지 않고 실패 카드로 보낸다. 빈 HWPX를 성공으로 주면 사용자는 변환된 줄 안다.

## 폴더 단위 작업과 변환 리포트

전환기의 실제 작업 단위는 "파일 하나"가 아니라 **"폴더 하나"**다. UI는 그 단위를 그대로 받는다.

- 폴더를 드롭하면 하위 폴더까지 재귀로 훑어 변환 대상만 큐에 담는다.
- **폴더 안 잡동사니는 조용히 거른다.** 지원하지 않는 파일 하나하나에 경고를 띄우면 폴더 드롭을 쓸 수 없다. 대신 몇 개를 제외했는지 요약 토스트로 한 번만 알린다. (파일을 직접 골랐을 때는 개별 안내를 계속한다 — 그건 사용자가 의도한 선택이기 때문이다.)
- 큐 상한은 100개, 총 200MB다. 개수만 제한하면 큰 파일 몇 개가 탭을 죽인다. 결과 Blob을 전부 메모리에 들고 있기 때문이다.
- 배치 중에도 메인스레드가 살아 있어야 한다. 변환 루프는 파일마다 `await`로 이벤트 루프에 양보한다. `npm run test:performance`가 100개 변환 중 UI 심박을 재고, 기대치의 50% 아래면 실패시킨다.

### 변환 리포트

기관이 문서를 일괄 전환할 때 필요한 것은 파일만이 아니라 **무엇이 어떻게 처리됐는지의 기록**이다.

- 배치 결과 카드에 `📋 변환 리포트`(CSV) 버튼을 둔다. 주 행동(전체 ZIP)보다 시각적 무게를 낮춘다.
- **실패한 행을 반드시 포함한다.** 성공만 담으면 리포트가 거짓말이 된다. 실패 행에는 다음 행동(`classifyConversionError`의 안내)을 함께 적는다.
- 출력 파일명은 **ZIP 안에서 실제로 쓰이는 이름**을 적는다. 확장자만 다른 동명 파일이 섞이면 ZIP은 이름을 바꿔 담는데, 리포트가 바뀌기 전 이름을 적으면 둘이 어긋나 대조가 안 된다.
- CSV는 BOM으로 시작한다. 없으면 Excel에서 한글이 깨진다.

## 규정 기반 변환은 근거를 보여준다 (공문서 항목)

공문서 항목 들여쓰기는 「행정업무의 운영 및 혁신에 관한 규정」에 근거한 변환이다. 추측이 아니므로 **근거를 밝히고**, 무엇을 했는지 계수로 보여준다.

- 결과 카드의 `result-gov-note`는 규정 이름을 명시하고, 인식한 항목 수·최대 단계·사용된 기호·붙임 건수·끝. 표시 여부를 적는다.
- 정보색(`--c-primary`)을 쓴다. PDF 추론 패널(경고색)과 달리 이건 **추측이 아니라 규정 적용**이므로 경고로 보이면 안 된다.
- 적용된 항목이 0건이면 패널을 띄우지 않는다. "0개 적용했습니다"는 잡음이다.
- 옵션 기본값은 **끔**이다. 일반 문서에도 "1."로 시작하는 줄은 흔하고, 켜져 있으면 원문보다 나빠진다. golden이 이 기본값을 고정한다.
- 이 기능이 실패해도 변환 자체는 계속한다. 보조 기능이 주 산출물을 막으면 안 된다.

## 공문 구성요소는 역할만 표시한다

공문 구조 인식(수신·경유·제목·발신명의·시행·접수·공개 구분)에서 **모양을 바꾸는 것은 두 가지뿐**이다.

- **제목** → 문서 제목으로 승격 + 제목 스타일. 공문에서 제목은 문서를 대표하는 유일한 줄이므로 안전하다.
- **발신명의** → 가운데 정렬. 규정 서식이 그렇게 정한다.

나머지는 역할만 기록하고 배치를 건드리지 않는다. 그 줄들의 배치는 기관 서식마다 다르고, 추측해서 옮기면 원문보다 나빠진다.

결과 카드는 인식한 구성요소를 한국어 이름으로 나열하고, 제목을 승격했으면 그 사실과 제목을 함께 보여준다. 무엇을 건드렸는지 사용자가 알 수 있어야 한다.

## 관리자 고도화 계획 탭

관리자가 "지금 어디까지 왔고 다음에 뭘 할지"를 한 화면에서 판단하기 위한 패널이다. 업데이트 내역 모달의 관리자 전용 탭으로 둔다 — 별도 페이지를 만들면 진입점이 늘고 관리자 모드 가림이 한 군데 더 생긴다.

- **내용을 화면에 적지 않는다.** 전부 `roadmap.json`에서 온다. 화면과 데이터가 갈라지면 계획서를 보고 잘못된 판단을 하게 된다. golden이 렌더된 단계·항목·수동확인 개수를 `roadmap.json`과 대조해 이 규칙을 고정한다.
- **수동 확인 항목을 완료 목록보다 위에, 경고색으로 둔다.** 자동 게이트가 아무리 많아도 한컴 시각 확인을 대체하지 않는다는 것이 이 저장소의 황금률이고, 화면이 그 반대 인상을 주면 안 된다. golden이 DOM 순서를 검사한다.
- 완료 단계에는 **근거(버전·PR)를 함께 보여준다.** 증거 없는 "완료"는 주장일 뿐이다.
- 요약 타일의 "사람 확인"은 경고색을 쓴다. 나머지 숫자와 같은 색이면 "다 끝났다"로 읽힌다.
- 다음 후보는 가치·비용과 **선행 작업**을 함께 적는다. 무엇이 막고 있는지 없으면 우선순위를 못 정한다.

정합성은 `npm run test:roadmap`이 지킨다 — 적어둔 버전·PR이 changelog에 있는지, 게이트가 `package.json`에 있고 `test:release`에 편입됐는지, 참조 문서가 실재하는지.

## 모션 — 스프링과 진입 애니메이션

### 왜 스프링인가

기존 모션은 전부 `--trans: 0.18s ease` 하나였고 `cubic-bezier`가 0개였다. 선형에 가까운 이징은 "값이 바뀌었다"만 알릴 뿐, 눌림·되돌아옴·자리 잡음 같은 **물리적 인상**을 주지 못한다.

감쇠 스프링 `x(t) = 1 - e^(-ζωt)(cos ωd·t + (ζω/ωd)sin ωd·t)`를 풀어 CSS `linear()` 정지점으로 구웠다. Framer Motion의 스프링과 같은 식이며 **JS도 외부 의존성도 없다.**

이 앱의 CSP는 `script-src 'self'`라 GSAP·Framer Motion 같은 CDN 라이브러리를 애초에 실행할 수 없고, 빌드 과정이 없어 Tailwind도 쓸 수 없다. vendor 추가는 `commercial-gate`가 SHA-256으로 검사하는 공급망 항목이기도 하다. `linear()`는 그 제약 안에서 같은 결과를 낸다.

| 토큰 | 물리값 | 정착 | 오버슈트 | 쓰는 곳 |
|---|---|---|---|---|
| `--spring-snap` | k=900 c=38 | 304ms | 7.6% | 버튼·칩·토글 — 직접 조작 |
| `--spring-soft` | k=230 c=30 | 494ms | 0% | 진입·퇴장 — 넘김이 거슬리는 곳 |
| `--spring-bounce` | k=640 c=26 | 368ms | 15% | 모달·토스트 — 존재를 알릴 때 |

**정지점 값을 손으로 고치지 않는다.** 물리 파라미터에서 계산된 곡선이라 중간 값만 바꾸면 물리적으로 불가능한 움직임이 된다. 파라미터를 바꾸고 다시 계산할 것.

`@supports not (animation-timing-function: linear(0, 1))`로 구형 브라우저에서는 근사 베지어로 대체한다 — 느낌은 덜하지만 움직임이 사라지거나 튀지 않는다.

누를 때(`:active`)는 스프링을 쓰지 않는다. 60ms로 즉시 반응해야 "눌렸다"고 느끼고, 되돌아올 때만 스프링이 걸린다.

### 진입 애니메이션은 투명도를 건드리지 않는다

`opacity: 0`인 글자는 글자가 없는 것과 같다. 화면 아래에 있어 아직 드러나지 않은 본문이 접근성 검사에서 대비 0으로 잡히는데, **그건 오탐이 아니라 사실이다** — 그 순간 그 글자는 읽을 수 없다. 실제로 페이드 방식으로 만들었다가 axe `color-contrast`가 14곳을 잡아냈다.

그래서 이동·축소만으로 등장을 표현한다. 스프링의 감속이 충분히 강해서 페이드 없이도 자리를 잡는 인상이 난다.

### 진입 모션이 본문을 삼키지 않게

숨김 상태는 `html.js-reveal`이 있을 때만 적용한다. 이 클래스는 `initScrollAnimations()`가 붙인다. 스크립트가 실패하거나 `IntersectionObserver`가 없으면 클래스가 없으므로 **내용은 제자리에 그대로 있다.** 진입 모션 때문에 본문이 영구히 사라지는 것이 최악의 결과다.

추가로 (1) 첫 화면에 이미 들어와 있는 요소는 rAF에서 즉시 드러내고, (2) 6초 뒤에는 무조건 전부 드러내는 안전망을 둔다.

진입 경로는 **하나만** 둔다. 예전에는 `animation-timeline: view()`와 `IntersectionObserver` 두 갈래가 공존해 브라우저마다 다르게 움직였고, 어느 쪽이 실제로 도는지 알 수 없어 회귀도 잡히지 않았다.

## 시각적 계층 — 진한 덩어리는 하나만

화면에서 가장 진한 덩어리가 네다섯 개면 어디를 눌러야 할지 알 수 없다.

- **solid 브랜드 채움은 주 행동(변환 버튼) 하나만 쓴다.**
- 선택된 옵션(`.seg-btn.is-active`)은 '선택됨'이지 '주 행동'이 아니다. 틴트 배경 + 안쪽 테두리로 표시한다. 예전에는 solid 브랜드색이라 `세로/가로` 같은 설정 버튼이 실행 버튼과 같은 무게였고, 정작 CTA는 비활성이라 더 흐렸다.
- CTA 위에는 여백을 넉넉히 둔다. 옵션 더미와 붙어 있으면 '설정의 일부'로 읽힌다.
- 비활성 → 활성으로 넘어가는 **그 순간에만** CTA가 한 번 튕긴다(`.is-ready`). 파일을 고른 직후가 이 화면에서 가장 중요한 전환 지점이다. 이미 활성인 상태에서 라벨만 바뀔 때는 튕기지 않는다 — 반복되면 잡음이 된다.

## 폼 컨트롤 높이는 명시값으로 고정한다

`select`·세그먼트·토글이 각자 `padding`으로 높이를 만들면 실제 렌더 높이가 **폰트 메트릭에 좌우된다.** 한글 웹폰트(Noto Sans KR)가 적용되기 전 39px, 적용된 뒤 41px로 달라져 같은 행의 컨트롤이 최대 3px 어긋났다. 폰트 로딩 타이밍에 따라 정렬이 달라지는 것은 받아들일 수 없다.

`--control-h: 40px`(작은 변형은 `--control-h-sm: 35px`)로 고정한다. `golden`의 `heightSpread` 검사가 이 정렬을 지키며, 실패 메시지에 실측값을 함께 출력해 무엇이 어긋났는지 바로 보이게 한다.

## 히어로는 제품명이 아니라 가치를 말한다

한동안 히어로에는 `TO HWPX - 한글로`라는 **제품명 한 줄뿐**이었다. 처음 온 사람은 그 문장에서 무엇을 하는 도구인지, 왜 이걸 써야 하는지 알 수 없다. 597px를 쓰면서 판단에 필요한 정보를 하나도 주지 않은 셈이다.

히어로는 세 가지를 순서대로 말한다.

1. **무엇을 하는가** — `PDF·워드·엑셀 문서를 한글(HWPX)로 바꿉니다.`
2. **왜 이게 필요한가** — `한컴오피스 없이, 브라우저에서 바로.`
3. **무엇이 다른가** — `.hero-points` 세 줄(로컬 처리 · 폴더 100개 · 공문서 규정)

이 셋은 마케팅 문구가 아니라 **실제 구현된 기능**이어야 한다. 없는 기능을 적으면 결과 카드 문구와 어긋나고, 그 순간 나머지 설명도 못 믿을 말이 된다.

**한국어 줄바꿈**: `.hero-lead`에는 `word-break: keep-all`이 필수다. 없으면 `바꿉니다`가 `바꿉/니다`로 잘린다. 두 문장이 한 줄에 섞여 문장 중간에서 접히지 않도록 둘째 문장은 `<span>`으로 제 줄에 둔다.

## 첫 방문을 모달로 가로막지 않는다

예전에는 첫 방문 700ms 뒤에 안내 모달이 자동으로 떴다. 처음 온 사람은 히어로도 드롭존도 보기 전에 모달부터 치워야 했다.

이는 이 저장소의 UX 불변식 — "첫 화면의 주 행동은 드롭존이고, 보조 안내가 그보다 먼저 시선을 빼앗지 않는다" — 을 정면으로 어긴다. **모달은 버튼보다 더 강하게 시선을 빼앗는다.** 게다가 히어로가 무엇을 하는 도구인지 이미 설명하고 3단계 안내도 페이지 안에 그대로 있어서, 모달이 하던 말은 중복이었다.

그래서 **막지 않고 가리키기만 한다**: 도움말 버튼에 점(`.has-nudge`) 하나를 붙인다. 필요한 사람은 누르고, 바로 변환하려는 사람은 방해받지 않는다. 점은 도움말을 열거나 파일을 고르면 사라진다(`clearOnboardingNudge()`) — 도구를 쓸 줄 아는 사람에게 계속 남아 있으면 그것도 잡음이다.

## 글자 크기 하한 — 11px

한글은 영문보다 획이 많아 **11px 아래에서는 실질적으로 읽을 수 없다.** 여백 미니맵 라벨이 8.64px, 단계 라벨이 9.6px까지 내려가 있었다. 읽으라고 넣은 글자가 읽히지 않으면 자리만 차지하는 것이다.

`--fs-2xs`를 **0.7rem(11.2px)** 로 두고 이것을 하한으로 삼는다. 더 작은 값이 필요해 보이면 글자를 줄일 게 아니라 **그 정보가 정말 필요한지** 다시 본다. `tests/golden.js`가 본문 leaf 요소의 실측 `font-size`를 훑어 11px 미만을 잡는다.

## 섹션 세로 리듬은 하나의 값에서 나온다

섹션마다 패딩이 제각각(히어로 104/72 · 변환기 64/64 · 포맷 16/51.2 · 푸터 72/28)이면 스크롤할 때 박자가 어긋나 페이지가 조립품처럼 읽힌다.

`--section-y: clamp(48px, 6vw, 72px)` 하나에서 시작하고, **의도적으로 다른 곳만** 따로 준다(히어로 상단은 더 크게, 푸터 하단은 더 작게). 미디어 쿼리로 섹션별 패딩을 다시 적는 대신 `clamp`가 반응형을 처리하게 둔다 — 오버라이드가 쌓이면 어느 값이 이기는지 추적할 수 없다.

## PDF 변환 품질은 세 숫자로 말한다

"PDF 변환율 100%"는 **무엇을 세는지 정하기 전에는 검증할 수 없는 주장**이다. PDF에는 문단·표·제목이라는 개념이 없고 "이 좌표에 이 글자를 그려라"만 있다. 그래서 하나의 숫자로 뭉치지 않고 셋으로 나눈다.

| 지표 | 뜻 | 성격 |
|---|---|---|
| **문자 보존** | PDF 글자 레이어의 글자가 결과에 남아 있는가 | **하한선**(점수가 아님) |
| **읽기 순서** | 그 글자들이 읽는 순서대로 있는가 | 2단·병합셀에서 무너진다 |
| **구조 정확도** | 제목·표·목록·그림·서식이 정답과 맞는가 | 단언 통과율 |

뭉치면 **글자를 잃고도 구조 점수로 가릴 수 있다.** 실제로 2단 문서는 글자 보존이 100%인데 좌우가 섞여 읽을 수 없었다 — 한 숫자였다면 문제가 보이지 않았다.

**문자 보존은 점수가 아니라 하한선이다.** 글자가 사라지는 것은 추론 실패가 아니라 버그다. 다만 기준은 *아무것도 안 사라짐*이 아니라 **조용히 사라진 것이 없음**이다. 쪽마다 반복되는 머리말·쪽번호는 본문이 아니라 쪽 장식이므로 일부러 뺀다. 그 대신 `audit.removedPageFurniture`에 무엇을 뺐는지 적고, 게이트가 그것과 대조한다. 선언된 제거는 통과하고, 선언 없는 유실은 실패한다.

**결과 카드에 고정 백분율을 쓰지 않는 원칙은 그대로다.** 위 숫자는 우리가 만든 픽스처에 대한 회귀 지표이지 사용자 문서의 품질 보증이 아니다. 쉬운 문서에서 나온 100%는 PDF 변환이 100%라는 뜻이 아니므로, 픽스처는 **일부러 어려운 것만** 모았다(서식·그림·병합셀·좁은 괘선표·2단·반복 머리말).

**모르는 것은 모른다고 둔다.** 한글은 줄이 바뀔 때 공백이 있었는지 PDF만으로는 알 수 없다(`왼쪽 단을`+`모두` vs `추진하기 위`+`하여` — 끝나는 x 좌표까지 같다). 넣으면 `위 하여`, 안 넣으면 `단을모두`로 손실이 대칭이라, 근거가 생기는 라틴 문자에만 공백을 넣고 나머지는 알려진 한계로 남긴다. 게이트를 통과시키려고 한쪽을 고르지 않는다.
